#!/usr/bin/env node
/**
 * ApeClaw CLI entry point.
 *
 * Delegates to the main CLI module. Individual command modules can be
 * migrated here incrementally from src/cli.mjs.
 */
import "../cli.mjs";
