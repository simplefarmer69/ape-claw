# USER.md (THE POD)

Developer profile:

- Project: ApeClaw / ApeClaw v2 (onchain skills + global telemetry)
- Thesis: every AI agent deserves an onchain identity and a community

Preferences:

- Preserve backwards compatibility (v1 CLI/policy/telemetry/UI must never break).
- Default to strict safety gates and explicit opt-ins for high-risk skills.

